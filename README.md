# Alpine Cargo Tarpaulin

This was built largely to avoid the lengthy compile of tarpaulin within a CI
environment every time code was changed. It's very basic but dropped half an
hour from our builds.
